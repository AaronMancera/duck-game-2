using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JugadorControlador : MonoBehaviour
{
    /// <summary>
    /// En este controlador vamos a programar todo lo relacionado con el jugador.
    /// 
    /// El jugador pordra moverse de izq a der, saltar y agacharse. Además este agacharse debera detectar en que suelo esta y si es traspasable o no
    /// 
    /// Dentro de este script debe de estar relacionado con InteraccionScript para poder usar todos los objetos y armas que se encuentran en el suelo
    /// 
    /// La recogida de armas/objetos sera mediante un trigger que detecte en la zona de spawn de armas/objetos
    /// </summary>
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
