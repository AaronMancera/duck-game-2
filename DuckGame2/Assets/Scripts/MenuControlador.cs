using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuControlador : MonoBehaviour
{
    /// <summary>
    /// Controlara el menu ui
    /// 
    /// Habra un boton que llevara al lobby y de ahi ne iniciara el juego al estar preparado los dos jugadores
    /// </summary>
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
