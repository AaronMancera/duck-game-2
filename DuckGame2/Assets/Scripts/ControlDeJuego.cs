using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlDeJuego : MonoBehaviour
{

    /// <summary>
    /// Va a recoger la vida de los jugadores y va a detectar y realizar la accion de reiniciar la escena
    /// 
    /// IMPORTANTE, aplicar un patron singleton para no perder los datos de la partida actual (puntuacion)
    /// 
    /// Va a controlar si se ha acaabado la partida (mejor de 3 o mejor de 5)
    /// 
    /// Tambien esto al acabara devolvera a la scena del menu
    /// </summary>
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
