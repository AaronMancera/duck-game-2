using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InteraccionScript : MonoBehaviour
{
    /// <summary>
    /// Este script sera utilizado por el jugador JugadorControlador.cs
    /// con el boton predefinido del input de Action1 vamos a realizar las acciones de disparar y usar objetos recogidos por el jugador
    /// </summary>
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
